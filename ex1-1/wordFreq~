#!/bin/sh
rm "output.txt"
while read line; do
	for word in $line; do
		if grep [[ "$word" "output.txt" ]]; then 
			 echo "$word $2+1" >> "output.txt"
		else 
			echo "$word  1" >> " output.txt"
		fi
	done
done < "test_data"
